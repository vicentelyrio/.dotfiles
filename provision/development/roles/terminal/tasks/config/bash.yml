---
- name: BASH - symlink config
  ansible.builtin.file:
    src: "{{ playbook_dir }}/development/roles/terminal/files/bash/{{ item }}"
    path: ~/{{ item }}
    state: link
    follow: false
    force: true
  loop:
    - .bashrc
