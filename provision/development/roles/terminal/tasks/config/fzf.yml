---
- name: FZF - symlink files
  ansible.builtin.file:
    src: "{{ playbook_dir }}/development/roles/terminal/files/fzf/{{ item }}"
    path: ~/{{ item }}
    state: link
    follow: false
    force: true
  loop:
    - .fzf.zsh
    - .fzf.bash
