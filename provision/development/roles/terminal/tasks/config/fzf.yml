- name: FZF - symlink files
  ansible.builtin.file:
    src: "{{ playbook_dir }}/roles/terminal/files/fzf/{{ item }}"
    dest: ~/{{ item }}
    state: link
    follow: false
    force: true
  with_items:
    - .fzf.zsh
    - .fzf.bash
