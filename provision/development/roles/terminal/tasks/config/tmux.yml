- name: Tmux - create config directory
  ansible.builtin.file:
    path: "{{ home_dir_result.stdout }}/.config/tmux"
    state: directory
    mode: "0644"

- name: Tmux - symlink files
  ansible.builtin.file:
    src: "{{ playbook_dir }}/roles/terminal/files/tmux/{{ item }}"
    dest: "{{ home_dir_result.stdout }}/.config/tmux/{{ item }}"
    state: link
    follow: false
    force: true
  with_items:
    - .tmux.conf
