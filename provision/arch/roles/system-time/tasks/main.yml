- name: Ntp - install daemon
  pacman:
    name: ntp
    state: present
  register: install
  until: install is success
  retries: 10
  delay: 2
  notify: rkhunter propupdate

- name: Ntp - configure swiss metas atomic clocks
  ansible.builtin.copy:
    src: ntp.conf
    dest: /etc/
    mode: "0644"
  notify:
    - restart ntpd
